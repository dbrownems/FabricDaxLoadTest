# Build outputs
**/bin/
**/obj/
**/publish_test/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# OS
.DS_Store
Thumbs.db

# Tokens & local artifacts
*.token
logs/

# Personal AI agent instructions (not for sharing)
.copilot/
AGENTS.local.md


# Local smoke artifacts
artifacts/
tmp/

# build artifact from Deploy-LoadTests.ps1 (legacy zip, replaced by
# the fat wheel in v0.5.0+ — gitignored so a stale local copy doesn't
# get committed if the user has a pre-v0.5.0 deploy script lying around)
loadgen-bin.zip

# .NET LoadGen publish output, staged into the wheel by
# Deploy-LoadTests.ps1 / release.yml before `python -m build`. Build
# output, never source-controlled.
src/fdlt_runtime/loadgen/


# Python build outputs
dist/
build/
*.egg-info/
src/fdlt_runtime/_version.py


# Python bytecode
__pycache__/
*.pyc

